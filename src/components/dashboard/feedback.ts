// Shim — moved to src/components/ui/feedback (2026-07 follow-ups) so the toast
// system is decoupled from dashboard i18n. Kept for the old import path
// (src/components/dashboard/__tests__/feedback.test.ts and any other relative import).
export * from '@/components/ui/feedback'
